apiVersion: v1
kind: Service
metadata:
  name: service-template
  labels:
    app: service-template
spec:
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: service-template
