apiVersion: v1
kind: Service
metadata:
  name: plugin-directory
  labels:
    app: plugin-directory
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: plugin-directory
